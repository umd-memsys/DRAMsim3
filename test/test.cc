#include "catch.hpp"
#include "timing.h"

TEST_CASE("Timing Relations", "[timing]") {
    // dramcore::Config config;
    // dramcore::Timing timing(config);
    //REQUIRE( 0 == timing.read_to_read_s);
    REQUIRE( 0 == 0 );
}